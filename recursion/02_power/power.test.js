const power = require("./power");

test("2 to the power of 4 to equal to 16", () => {
  expect(power(2, 4)).toBe(16);
});
test("2 to the power of 3 to equal to 8", () => {
  expect(power(2, 4)).toBe(16);
});
test("2 to the power of 2 to equal to 4", () => {
  expect(power(2, 4)).toBe(16);
});
test("2 to the power of 1 to equal to 2", () => {
  expect(power(2, 4)).toBe(16);
});
test("2 to the power of 0 to equal to 1", () => {
  expect(power(2, 4)).toBe(16);
});
